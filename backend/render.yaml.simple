# Simplified render.yaml for reference
# For first deployment, use Manual Setup in Render dashboard instead
# This file is for reference only

services:
  - type: web
    name: meditrack-backend
    env: java
    rootDir: backend
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/*.jar

